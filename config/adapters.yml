claude:
  personalisation:
    - dest: '~/.claude/CLAUDE.md'
      target: dist/claude/CLAUDE.md
  skills:
    - dest: '~/.claude/skills/'
      target: skills/
  mcp:
    scope: user
    enable: [shared-memory, project-memory]

codex:
  personalisation:
    - dest: '~/.codex/AGENTS.md'
      target: dist/codex/AGENTS.md
  skills: native
  mcp:
    home: '~/.codex'
    enable: [shared-memory, project-memory]

opencode:
  personalisation:
    - dest: '~/.config/opencode/AGENTS.md'
      target: dist/opencode/AGENTS.md
  skills: native
  mcp:
    enable: [shared-memory, project-memory]

zed:
  personalisation:
    - dest: '~/.config/zed/AGENTS.md'
      target: dist/zed/AGENTS.md
  skills: native
  mcp:
    enable: [shared-memory, project-memory]

cursor:
  personalisation:
    - dest: '~/.cursor/rules/'
      target: dist/cursor/rules/
  skills:
    - dest: '~/.cursor/skills/'
      target: skills/
  mcp:
    enable: [shared-memory, project-memory]
