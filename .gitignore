node_modules/
hindsight/logs/
hindsight/env.local
hindsight/codex-home/
hindsight/*.plist
hindsight/.sync-cache.json
personalisation/*
!personalisation/.gitkeep
# Machine-local vault symlinks are ignored; real source files are tracked explicitly.
!personalisation/Shared memory.md
.DS_Store
mcp/.state.json

# Warp reads its global rules from this repo root; the file is a rendered link.
/AGENTS.md
